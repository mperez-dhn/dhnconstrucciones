# DHN Construcciones — Sitio web

Landing page de DHN Desarrolladora S.R.L. Sitio estático (HTML/CSS), sin build.

## Estructura
- `index.html` — página principal (español)
- `assets/img/` — fotos reales de obras (pendiente de cargar)

## Deploy (Cloudflare Pages + Git)
Repositorio conectado a Cloudflare Pages. Cada push a `main` publica automáticamente.
- Build command: *(ninguno)*
- Build output directory: `/` (raíz)

## Pendientes
- Cargar fotos reales en `assets/img/` (hero, Embalse, Puente UBY, galpón, enfriadoras, logos de clientes).
- Versión en inglés (`/en/`).
- Capability statement (PDF descargable) para procurement.
